
  # HabitZen Beta 1.2 (Community) (copia)

  This is a code bundle for HabitZen Beta 1.2 (Community) (copia)

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  